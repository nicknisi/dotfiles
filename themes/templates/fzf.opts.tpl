# {{ name }} — fzf palette, rendered by bin/theme.
--color=bg:-1,bg+:{{ lighter_background }},fg:{{ foreground }},fg+:{{ bright_foreground }},hl:{{ magenta }},hl+:{{ bright_magenta }},info:{{ muted }},prompt:{{ accent }},pointer:{{ accent }},marker:{{ green }},spinner:{{ cyan }},header:{{ yellow }},border:{{ muted }}
